```Mermaid
sequenceDiagram
    前端 ->> 服务端 : 1. 请求生成临时访问S3的Token
    服务端 -->>  前端 : 2. 返回零食访问S3 Token
    前端 ->> S3: 3. 使用临时Token上传文件(直连)
    S3 -->> 前端:  4. 上传完成，返回文件ID
    前端 ->> 服务端: 5. 发送文件ID + 文件名
    服务端 ->> 服务端: 6. 保存文件ID和文件名到数据库
    服务端 ->> S3: 7. 内网：根据文件ID获取文件
    服务端 ->> 服务端: 8. 解析拉取到的文件内容
  
```