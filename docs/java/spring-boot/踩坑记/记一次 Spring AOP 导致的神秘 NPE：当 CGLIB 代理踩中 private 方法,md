## 事故现场

某天排查日志，发现了一个令人百思不得其解的 NPE 异常：

```java
java.lang.NullPointerException: Cannot invoke "org.springframework.data.redis.core.StringRedisTemplate.opsForValue()" because "this.stringRedisTemplate" is null
```

第一反应是：Redis 挂了？ 但仔细一看报错位置，竟然是 RedisTemplate 变量本身为 null！
这就奇怪了，报错的类明明标注了 @Service，属性上也加了依赖注入注解，Spring 怎么可能把未注入依赖的 Bean 丢出来使用呢？
在深入追踪代码后，发现该类的写法非常“别致”，类似于通过静态方法去调用工具类封装的代理对象。

## 代码复现

为了彻底搞清原委，我们抽象出一个极简的 Demo（排除了所有业务干扰）。

### 1. 目标服务类 (DiscountService)

该类试图提供一个 static 方法供外部直接调用，内部通过 SpringContextUtil 拿到容器中的 Bean，再调用一个 private 的内部处理逻辑：

```java
@Service
public class DiscountService {

    @Autowired
    private StringRedisTemplate stringRedisTemplate;

    // 1. 对外暴露静态入口
    public static boolean isEnable(String key) {
        // 从 Spring 容器中获取当前 Bean 并调用内部逻辑
        return SpringContextUtil.getBean(DiscountService.class).checkInternal(key);
    }

    // 2. 内部私有校验方法
    private boolean checkInternal(String key) {
        // 💥 这里抛出 NPE！stringRedisTemplate 为 null！
        return "true".equals(stringRedisTemplate.opsForValue().get(key));
    }
}
```


2. 切面类 (LogAspect)

假设项目中有一个看似无关的切面（例如耗时统计或日志拦截），匹配了所有 @Service 标注的类：

```java
@Aspect
@Component
public class LogAspect {

    @Pointcut("within(@org.springframework.stereotype.Service *)")
    public void servicePointcut() {}

    @Around("servicePointcut()")
    public Object logAround(ProceedingJoinPoint joinPoint) throws Throwable {
        return joinPoint.proceed();
    }
}
```

3. 触发调用

```java
// 在任意地方调用静态方法
boolean active = DiscountService.isEnable("USER_DISCOUNT_SWITCH");
```

三、 根因深度剖析

为何依赖注入会“失效”？要理解这一点，我们需要拆解 Spring AOP 的底层创建过程 与 Java 语言特性。


1. 堆内存里的“双胞胎”
由于 DiscountService 被切面 LogAspect 匹配到了，Spring 在启动时并不会把原始的 DiscountService 实例暴露给外部，而是通过 CGLIB 创建了一个代理对象。
此时，JVM 内存中实际上存在两个角色：

```
  [ Target 目标实例 ]  ───> 原始 DiscountService，依赖注入成功 (stringRedisTemplate ≠ null)
  [ Proxy  代理实例 ]  ───> CGLIB 生成的子类实例，依赖未注入 (stringRedisTemplate == null)
```

提示：Spring 只会为 Target 目标实例完成依赖注入；Proxy 代理实例本质上只是一个壳，其属性字段均维持默认值（即 null）。

2. CGLIB 的原理与 private 方法的宿命


CGLIB 是通过继承目标类并重写（Override）其方法来实现拦截转发的：
•	public / protected 方法：CGLIB 可以重写它们。当调用 proxy.publicMethod() 时，拦截器捕捉请求，并将调用转发给带有完整依赖的 Target 实例 执行。
•	private 方法：在 Java 语法中，private 方法无法被子类继承，更无法被重写！
因此，CGLIB 完全无法拦截 private 方法。


3. 崩溃的终极链条


当我们执行 DiscountService.isEnable("KEY") 时，调用链如下：


```
DiscountService.isEnable("KEY")
   │
   ├──> 1. SpringContextUtil.getBean(DiscountService.class)
   │       └─ 拿到的是容器中的代理对象 【Proxy 实例】
   │
   ├──> 2. Proxy.checkInternal("KEY")
   │       ├─ CGLIB 无法重写 private 方法，拦截器失效！
   │       └─ 直接在 【Proxy 实例】 上强制执行原始 private 方法代码
   │
   └──> 3. 执行 this.stringRedisTemplate.opsForValue()
           ├─ 此时 this 指向的是 【Proxy 实例】
           └─ Proxy 上的 stringRedisTemplate 字段未注入（为 null） ───> 💥 触发 NPE！
```


四、 避坑指南与最佳实践

知道了原理，修复方案也就清晰了。

1. 应急修复：改 private 为 public（或 package-private）
将 checkInternal 的可见性提高：

```java
// 改为 public 后，CGLIB 能够成功重写该方法，并将请求转发给真正注入了依赖的 Target 实例
public boolean checkInternal(String key) {
    return "true".equals(stringRedisTemplate.opsForValue().get(key));
}
```

2. 根本解决：摒弃“伪静态”调用，回归标准依赖注入（DI）


在 Spring 范式中，非常不推荐在 @Service 中混用 public static 方法与 getBean()。这不仅破坏了依赖倒置原则，还极易触发代理机制的盲区。


```java
// 1. 标准 Service 编写
@Service
public class DiscountService {

    @Autowired
    private StringRedisTemplate stringRedisTemplate;

    public boolean isEnable(String key) {
        return "true".equals(stringRedisTemplate.opsForValue().get(key));
    }
}

// 2. 业务层通过标准 @Autowired / @Resource 注入使用
@Service
public class OrderService {

    @Autowired
    private DiscountService discountService;

    public void processOrder() {
        if (discountService.isEnable("USER_DISCOUNT_SWITCH")) {
            // ...
        }
    }
}
```

## SkyWalking 这类 APM 工具会出现类似问题吗？

SkyWalking 与 Spring AOP（CGLIB）在底层实现机制上的本质差异**：

| 维度  | Spring AOP (CGLIB) | SkyWalking (Java Agent) |
| --- | --- | --- |
| **底层原理** | **动态生成子类（Subclass Proxy）** | **字节码改写（Bytecode Instrumentation）** |
| **核心技术** | CGLIB / ASM（生成新 `.class`） | Java Agent + ByteBuddy / ASM |
| **实例数量** | **2 个**（Target 目标实例 + Proxy 代理实例） | **1 个**（只有原生的 Target 实例） |
| ** private 限制** | 无法重写 `private` 方法，导致执行留在未注入的 Proxy 上 | 直接修改原字节码指令，**不受访问修饰符限制** |

---

#### ① 没有“代理实例”，只有一个真实的 Bean

SkyWalking 是通过 Java Agent 在类加载（Class Loading）阶段，直接对目标类的字节码进行修改（In-place Modification）。

它不会在 JVM 中额外创建一个“子类代理对象”。因此，在内存中永远**只有一个**由 Spring 管理并完成了依赖注入的 `DiscountService` 实例。不管你怎么调用、谁来调用，`this.stringRedisTemplate` 指向的永远是那个完成了 `@Autowired` 注入的正确属性

② 字节码织入绕过了 Java 语法的继承限制

CGLIB 依赖 Java 的**继承与重写（Override）**机制，因而受制于 `private` 方法不可重写的语法规则。

而 SkyWalking 是直接在已有方法的字节码指令集（Bytecode Instructions）中，强行插入 Trace 链路追踪的逻辑（比如在 `MethodEnter` 和 `MethodExit` 时插桩）。这种方式**对 `public`、`private`、`static` 甚至 `final` 方法一视同仁**，根本不需要依赖重写，自然也就不会破坏原本的代码执行上下文。


五、 总结


1.	CGLIB 无法拦截 private / final 方法。
2.	通过 getBean() 拿到的是代理对象，对代理对象直接调用 private 方法会在代理对象自身上下文中执行，此时其内部 @Autowired 属性均为 null。
3.	遵循 Spring 官方规范，尽量避免在 Service 内部封装静态方法反向获取 Bean。遵从干净的依赖注入，就能完美避开这类隐藏极深的诡异 NPE。